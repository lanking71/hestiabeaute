빌드 성공입니다. 생성된 파일 목록을 정리합니다.

  ---
  완료된 Docker 구성

  생성된 파일

  D:\hestiabeaute\
  ├── docker-compose.yml          ← 메인 오케스트레이션
  ├── .env.docker                 ← 백엔드 환경변수 (운영 전 변경 필요)
  ├── nginx/
  │   └── nginx.conf              ← 리버스 프록시 설정
  ├── backend/
  │   ├── Dockerfile
  │   ├── .dockerignore
  │   └── entrypoint.sh           ← 컨테이너 시작 시 DB 초기화 + 시드
  └── frontend/
      ├── Dockerfile              ← 멀티스테이지 빌드 (standalone)
      └── .dockerignore

  실행 방법

  # 로컬에서 실행 시 (기본)
  docker compose up -d --build

  # 원격 서버 배포 시 — IP 주소를 실제 서버 IP로 변경 후
  # docker-compose.yml → NEXT_PUBLIC_API_URL: "http://서버IP:8082/api"
  docker compose up -d --build

  접속 주소

  - 웹사이트: http://localhost:8082
  - 관리자: http://localhost:8082/admin/login (admin / hestia1234!)
  - API 문서: http://localhost:8082/docs

  주요 설계 포인트

  - SSR API 분리: 서버사이드는 Docker 내부망 http://backend:8000/api 직접 호출, 브라우저는 nginx를 통한
  http://localhost:8082/api 사용
  - 데이터 영속화: SQLite DB와 업로드 이미지를 Named Volume으로 관리
  - 경량 이미지: Next.js standalone 출력으로 프론트 이미지 크기 최소화